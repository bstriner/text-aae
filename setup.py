from setuptools import setup, find_packages

setup(name='text_aae',
      version='0.0.1',
      install_requires=[],
      author='Ben Striner',
      url='https://github.com/bstriner/text-aae',
      packages=find_packages())
